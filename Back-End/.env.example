# Servidor
PORT=3333
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Auth
JWT_SECRET=troque-isso-antes-de-subir
JWT_EXPIRES_IN=8h
# Validade do login temporario (horas) ate o usuario definir usuario/senha definitivos
TEMP_LOGIN_TTL_HOURS=48

# Banco (TODO: quando escolher Prisma/Mongo/PG)
DATABASE_URL=

# Arquivos
STORAGE_DIR=./storage
